{% extends 'base.html.twig' %}

{% block body %}
    <h1>Customers list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Idclient</th>
                <th>Name</th>
                <th>Adress</th>
                <th>Email</th>
                <th>Phonenumber</th>
                <th>Firstname</th>
                <th>Numbercustommer</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for customer in customers %}
            <tr>
                <td><a href="{{ path('customer_show', { 'id': customer.id }) }}">{{ customer.id }}</a></td>
                <td>{{ customer.idclient }}</td>
                <td>{{ customer.name }}</td>
                <td>{{ customer.adress }}</td>
                <td>{{ customer.email }}</td>
                <td>{{ customer.phonenumber }}</td>
                <td>{{ customer.firstname }}</td>
                <td>{{ customer.numbercustommer }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('customer_show', { 'id': customer.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('customer_edit', { 'id': customer.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('customer_new') }}">Create a new customer</a>
        </li>
    </ul>
{% endblock %}
